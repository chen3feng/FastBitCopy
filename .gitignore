# ---- UE build artifacts ----
Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/
*.sln
*.suo
*.xcodeproj/
*.xcworkspace/

# ---- CI standalone harness build output ----
CI/build/
CI/build-*/
# Repo-root build dirs from ad-hoc local runs (cmake -B build-* at repo root)
/build/
/build-*/

# ---- Editor / IDE ----
.vs/
.vscode/
.idea/
*.user

# ---- OS junk ----
.DS_Store
Thumbs.db
  
# Agent scratch directory (local only, never committed)  
.agent/  

# Local environment overrides
.env

# TestHost UE-generated config (DefaultEngine.ini, DefaultInput.ini, etc.)
TestHost/Config/

# TestHost plugin root junction/symlink (created by build scripts)
# No trailing slash: on Windows it's a directory junction, on Linux/Mac a symlink file.
TestHost/.plugin_root
